#include <stdio.h>

#define MAXSIZE 50

int main()
{
	char text[MAXSIZE];
	int counter = 0;
	int i; 
	
	char test = 'a';
	
	printf("Enter String: ");
	//gets(text);
	scanf("%s", text);
	printf("%s\n", text);
	
	for(i = 0; (i < MAXSIZE) && (text[i] !='\0'); i++)
	{
	 if(text[i] == test)
	 	counter++;
	}
	printf("Count: %d\n", counter);
}
